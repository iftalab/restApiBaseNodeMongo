Rest API with Node js and MongoDB

Node js framework : express
MongoDB driver : mongoose
MongoCloud : Atlas

This project includes a base model and basic CRUD for that base model in route

apiGenerator.py generates other model and route with basic CRUD 
give it the model name and schema definitaion and run

apiRemover.py removes model and route as per your need. just give it the model name and run